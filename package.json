{
  "name": "mi-app",
  "version": "1.0.0",
  "scripts": {
    "build": "ng build --prod",   // o vite build / react-scripts build
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
